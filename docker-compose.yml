services:
  ai-llm-api:
    build: .
    image: ghcr.io/cjx0712/ai-llm-api:latest
    ports:
      - "8000:8000"
    volumes:
      - ./models:/models:ro
    environment:
      - MODEL_PATH=/models/model.gguf
      - N_THREADS=4
    restart: unless-stopped
